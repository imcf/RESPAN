[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "respan"
version = "0.1.0"
description = "RESPAN - Restoration Enhanced Neuron and Spine Analysis"
requires-python = ">=3.9"